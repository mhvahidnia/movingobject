function relations = MakeRandomDIS( h )

relations=cell(2,h);



for i=1:length(relations)
    relations{1,i}=i;
    relations{2,i}='DIS';
    % relations{2,i}=i;
    % relations{3,i}=i;
    
    
end


end


